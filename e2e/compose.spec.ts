import { test } from "@playwright/test";

test.describe("Compose releases and drift", () => {
  test.skip("seed compose release catalog is visible", async () => {
    // Pending: compose release catalog not yet on new dashboard/pages
  });

  test.skip("compose drift inspector shows seed drifts", async () => {
    // Pending: compose drift inspector not yet built
  });

  test.skip("queue a compose release", async () => {
    // Pending: compose release form not yet on new pages
  });
});
