import { test } from "@playwright/test";

test.describe("Approval workflows", () => {
  test.skip("request approval for backup restore → approve", async () => {
    // Pending: approval workflow UI not yet on new pages
  });

  test.skip("seed approval data is present", async () => {
    // Pending: approval queue not yet on new pages
  });
});
